spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ratel_mind}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:ura123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:qquizzzin@gmail.com}
    password: ${EMAIL_PASSWORD:smjc auwv vuqf yktn}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${PORT:8080}

# APP_CORS_ALLOWED_ORIGINS=http://localhost:5173,https://ratelmind.com
app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:*}
  email-receiver: ${EMAIL_RECEIVER:kikiygigiy@gmail.com}